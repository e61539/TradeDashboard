import SwiftUI

struct BuyLowView: View {
    var body: some View {
        VStack {
            Text("BuyLow Logs")
                .font(.title2)
                .bold()

            Text("Placeholder view")
                .foregroundColor(.secondary)
        }
        .padding()
        .navigationTitle("BuyLow Logs")
    }
}
